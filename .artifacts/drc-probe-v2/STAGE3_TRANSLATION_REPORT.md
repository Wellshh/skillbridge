# Stage III-A translation analysis

The raw source is [run-29568](D:/AutoPlacer/skillbridge/.artifacts/drc-probe-v2/results/stage3-translation/run-29568-1789228092086311600). The offline reduction is [stage3_translation_analysis.json](D:/AutoPlacer/skillbridge/.artifacts/drc-probe-v2/results/stage3-translation-analysis/stage3_translation_analysis.json). It contains all nine cases, gate verdicts, actual DTOs, and the recorded relative Fraction geometry fields.

The three exact translations are `(0,0)`, `(-300,-300)`, and `(-600,-500)`, each for flag, clean, and the fixed control Cy. `flag_T0_0` and `clean_T0_0` are the origin references. Root's raw audit gives the clearance deltas for the translated flag/clean constructions as `13/4503599627370496` for T=(-300,-300), about `2.89e-15`, and `71/4503599627370496` for T=(-600,-500), about `1.58e-14`. These are observed geometry/transport differences and do not identify a comparator, quantizer, DRC integer rule, or epsilon.

The source uses `Fraction.from_float(float(base)) + exact integer translation`, converts that exact translated value to the wire float, and records actual relative coordinates with `Fraction.from_float(actual)-translation`. Arc center, radius and length remain separate fields; line and arc endpoints are not conflated.

## CLI precision evidence

The no-Allegro probe is [cli_argv_probe.py](D:/AutoPlacer/skillbridge/.artifacts/drc-probe-v2/cli_argv_probe.py). The III-A run manifest records the exact quoted argv strings `410.1249219650748` and `410.12492207444984`. The earlier unquoted PowerShell launch is retained in run-73932 as an approximate boundary input: its argv was shortened to `410.124921965075` and `410.12492207445`. This is a command-line argument conversion artifact, not a geometry or DRC conclusion.

All conclusions are conditional on the preserved raw DTO and gate evidence. No additional Allegro run was required for this report.

## Analysis v2 and reopen audit

The reproducible bits-to-Fraction analysis is [stage3_translation_analysis_v2.json](D:/AutoPlacer/skillbridge/.artifacts/drc-probe-v2/results/stage3-translation-analysis/stage3_translation_analysis_v2.json), generated by [stage3_translation_analysis_v2.py](D:/AutoPlacer/skillbridge/.artifacts/drc-probe-v2/stage3_translation_analysis_v2.py). It decodes every DTO coordinate, radius and width from binary64 bits, recomputes `cy-r-lineY-(arcWidth+lineWidth)/2`, compares each translated case with its group T0 using actual relative Fraction fields, and invokes the boundary gate on fresh item/snapshot evidence. Recomputed verdicts are six FLAGGED and three CLEAN; the three control cases are FLAGGED.

The exact FULL clearance fractions are retained per case in the JSON. Within each group, the T=(-300,-300) and T=(-600,-500) differences are preserved as Fraction deltas; these are geometry/transport observations and are not comparator attribution. The T0 flag and clean center-y bits match the boundary inputs used for the exact run.

The independent beta reopen reduction is included in the same JSON: both minus and plus have four unique objects, full DTO comparisons against the saved `after_dto`, and recomputed CLEAN gate verdicts. No saved board was written by the reopen runner.

CLI evidence is explicitly separated: `quoted.txt` records the unquoted PowerShell numeric invocation and shortened argv, while `quoted-explicit.txt` records the explicit quoted string invocation and full argv. The files were produced by [cli_argv_probe.py](D:/AutoPlacer/skillbridge/.artifacts/drc-probe-v2/cli_argv_probe.py); no Allegro process was involved.

## Analysis v2 correction

The authoritative gate module for recomputation is the immutable snapshot adjacent to the III-A raw manifest (`run-29568.../boundary_gate.py`), not the mutable working copy. The analysis now decodes all DTO bits to Fraction, recomputes FULL clearance, compares each group’s actual relative Fraction fields against T0, and records numeric Fraction deltas for every coordinate/geometry field. `relative_fraction_diff` is a value comparison record; it is not a claim that the displayed values are numeric differences.

T0 identity was checked against the independent boundary raw run `stage2-boundary/run-33608-1789226631112365500/cases.jsonl`: flag maps to `adaptive-007` and clean maps to `adaptive-010`; both have matching center bits, object names, and complete DTO equality. Width, radius, and other non-geometric rule/context claims remain governed by the preserved DTO and gate evidence.
