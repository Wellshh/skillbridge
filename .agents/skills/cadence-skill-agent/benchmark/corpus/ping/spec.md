# Task: __abpBenchPing

Write a SKILL proc `__abpBenchPing` that takes no arguments and returns `(plus 1 2)` (i.e. `3`). Pure SKILL, no `axl*` API. Must be loadable on Allegro 17.2.

Benchmark corpus task — see `version/spec.md` for the `agent.il` / `oracle.il` convention.
